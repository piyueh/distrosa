The scripts to obtain data and to plot figures are separated.

To get data:
```bash
$ python main.py
$ python train.py
```

A total of three `.dat` files will be created in the `figs` directory.

To plot figures:
```bash
$ python plot.py
```

Figures will be saved to the `figs` directory.
